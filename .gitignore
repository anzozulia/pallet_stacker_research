__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/
.idea/
.vscode/

# Cython build artefacts — generated by `pip install -e .`
# (the .pyx source files ARE tracked; everything Cython produces from them isn't)
pallet_packer/**/*.c
pallet_packer/**/*.so
pallet_packer/**/*.html
build/
dist/
*.egg-info/

# Ad-hoc report outputs from re-running scripts. The canonical reports
# live in docs/reports/. JSON checkpoints and galleries in results/ are
# tracked (research record).
results/*.md
/packing_result.json
.claude/
