__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/
.idea/
.vscode/

# Cython build artefacts — generated by `pip install -e .`
# (the .pyx source files ARE tracked; everything Cython produces from them isn't)
pallet_packer/**/*.c
pallet_packer/**/*.so
pallet_packer/**/*.html
build/
dist/
*.egg-info/

# Ad-hoc report outputs from re-running scripts. The canonical reports
# live in docs/reports/. JSON checkpoints and galleries in results/ are
# tracked (research record).
results/*.md
/packing_result.json
.claude/

# The API microservice is a SEPARATE project with its own git repo. It lives
# here for convenience during development but is intentionally excluded from
# the core algorithm repo. See pallet_packer_api_service/docs/.
pallet_packer_api_service/
